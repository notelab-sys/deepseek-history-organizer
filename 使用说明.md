# DeepSeek 对话记录整理工具——使用说明

## 用途

把 DeepSeek 对话记录整理成：

1. **可搜索网页**：按日期分组列出对话主题；关键词搜索（标题/正文/附件名称）；日期范围筛选、仅看含附件、导出当前检索结果为 JSON；双击 index.html 即可离线打开。
2. **Markdown 摘要目录**：index.md 总目录 + 每段对话一个文件。
3. **归纳文档**：把对话归纳成 Markdown 后转 Word（规范排版）或 PDF。
4. **速览卡片 / 登记表**：每段对话一张速览卡；归档文件夹自动生成登记表。

## 输入方式

- **方式 A（推荐）**：下载 DeepSeek「导出所有历史对话」的 zip/JSON，交给本工具本地解析、关键词检索筛选，再对选中的对话逐段分析。
- **方式 B（备选）**：DeepSeek 下载页无法筛选指定对话时，为要整理的对话生成分享链接（有效期约 7 天），批量抓取后处理。

## 处理规则

- 多段对话：**内容相近的可合并分析；主题不相同的必须单独分析**。
- 每段对话**全部内容整体分析**（不切分、不抽片段），一段对话对应一份完整文档。
- 文档按时间顺序连续编号（0001-日期-主题、0002-…），统一放进一个文件夹，便于检索。
- 归纳文档附录只列实质讨论主题：重复提问保留一条，"谢谢"等简单答复不列入，"请按 HTML/Word/PDF 格式输出"等格式生成类提问不列入。

## 三步流程

```bash
# 1. 抓取分享链接（方式 B；方式 A 可直接跳到第 2 步）
python scripts/fetch_share.py <分享链接或share_id> [...] -o share_data

# 2. 解析
python scripts/parse_deepseek.py <输入> -o conversations.normalized.json

# 3. 生成成果
python scripts/build_html.py conversations.normalized.json -o index.html
python scripts/build_markdown.py conversations.normalized.json -o digests
python scripts/build_cards.py cards conversations.normalized.json -o .
python scripts/extract_references.py conversations.normalized.json --append 归纳.md
python scripts/build_docx.py 归纳.md -o 归纳.docx
python scripts/build_pdf.py 归纳.md -o 归纳.pdf   # 需本机安装 Microsoft Edge
```

### 辅助工具

- 本地检索：`python scripts/search_conversations.py conversations.normalized.json --keywords "关键词" --date-from 2024-01-01 --match all --export selected.json`
- 主题相近提示（辅助合并决策）：`python scripts/suggest_similar.py conversations.normalized.json`
- 文件夹登记表：`python scripts/build_cards.py catalog <归档文件夹>`
- 附件正文提取：`python scripts/extract_documents.py <附件文件夹> -o docs_extracted.json --txt-dir docs`
- 参考文献半自动核查：`python scripts/verify_references.py references.md -o references.check.md`

## 脚本清单

| 脚本 | 作用 |
| --- | --- |
| fetch_share.py | 批量抓取分享链接（每条保存为对话 JSON） |
| parse_deepseek.py | 解析 zip/JSON，归一化对话与消息 |
| search_conversations.py | 本地关键词检索（日期范围、AND/OR、导出子集） |
| suggest_similar.py | 对话主题相近提示 |
| build_html.py | 生成可搜索网页（筛选/导出） |
| build_markdown.py | 生成 Markdown 摘要目录 |
| build_cards.py | 速览卡片（cards）与文件夹登记表（catalog） |
| extract_documents.py | 附件文档正文提取（Word/PDF/Excel 等） |
| extract_references.py | 提取对话中的网页链接与疑似文献引用 |
| verify_references.py | 参考文献半自动核查（Crossref/DOI） |
| build_docx.py | Markdown → Word（规范排版） |
| build_pdf.py | Markdown → PDF（需 Edge） |

## 使用注意

- 分享链接有效期约 7 天；分享接口不提供附件文件下载，公开论文可经开放获取渠道（PMC/期刊官网等）补全后提取正文。
- 所有 AI 给出的文献与结论，引用前务必自行核对（见[免责声明](免责声明.md)）。
